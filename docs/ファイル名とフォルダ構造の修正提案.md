# ファイル名とフォルダ構造の修正提案

**作成日**: 2026年1月13日  
**目的**: ダウンロードファイル名とフォルダ構造を検索条件に基づいて整理する

---

## 要件

1. **ファイル名**: `大分類_中分類_小分類_細分類_工事名_ファイル名` の形式
2. **フォルダ構造**: `大分類_中分類_小分類_細分類_工事名` のフォルダごとに保存

---

## 実装方法の提案

### 方法1: NamingクラスとDownloaderクラスを拡張（推奨）

#### メリット
- 既存のコード構造を維持
- 検索条件を明示的に渡せる
- テストしやすい

#### 実装手順

**1. `Naming`クラスに検索条件を渡せるように拡張**

```python
# src/core/naming.py

class Naming:
    """ファイル名を生成するクラス"""

    def __init__(self, naming_rule: str, logger: Logger = None, search_conditions=None):
        """初期化
        
        Args:
            naming_rule: 命名規則（テンプレート文字列）
            logger: ロガー
            search_conditions: 検索条件（SearchConditionsオブジェクト）
        """
        self.naming_rule = naming_rule
        self.logger = logger or Logger()
        self.file_utils = FileUtils()
        self.search_conditions = search_conditions

    def generate_filename(
        self, file_info: FileInfo, metadata: Dict[str, Any] = None, index: int = 0
    ) -> str:
        """ファイル名を生成"""
        # 検索条件から分類情報を取得
        context = self._build_context_from_search_conditions(file_info, metadata, index)
        
        # 命名規則に従ってファイル名を生成
        filename = self.naming_rule.format(**context)
        
        # 無効な文字を削除
        filename = self.file_utils.sanitize_filename(filename)
        
        # 拡張子を追加
        extension = file_info.get_file_extension()
        if extension and not filename.endswith(extension):
            filename += extension
        
        return filename

    def _build_context_from_search_conditions(
        self, file_info: FileInfo, metadata: Dict[str, Any] = None, index: int = 0
    ) -> Dict[str, Any]:
        """検索条件からコンテキストを構築"""
        context = {
            "filename": file_info.filename,
            "file_type": file_info.get_file_extension().replace(".", ""),
            "date": datetime.now().strftime("%Y%m%d"),
            "index": str(index),
        }
        
        # 検索条件から分類情報を取得
        if self.search_conditions:
            sc = self.search_conditions
            context["daibunrui"] = sc.hachu_daibunrui or ""
            context["chubunrui"] = sc.hachu_chubunrui or ""
            context["shoubunrui"] = sc.hachu_shoubunrui or ""
            context["saibunrui"] = sc.hachu_saibunrui or ""
            context["koji_name"] = sc.koji_name or ""
        else:
            context["daibunrui"] = ""
            context["chubunrui"] = ""
            context["shoubunrui"] = ""
            context["saibunrui"] = ""
            context["koji_name"] = ""
        
        # メタデータから追加情報を取得
        if metadata:
            context.update(metadata)
        if file_info.metadata:
            context.update(file_info.metadata)
        
        return context

    def generate_folder_name(self) -> str:
        """フォルダ名を生成（大分類_中分類_小分類_細分類_工事名）"""
        if not self.search_conditions:
            return ""
        
        sc = self.search_conditions
        parts = []
        
        if sc.hachu_daibunrui:
            parts.append(sc.hachu_daibunrui)
        if sc.hachu_chubunrui:
            parts.append(sc.hachu_chubunrui)
        if sc.hachu_shoubunrui:
            parts.append(sc.hachu_shoubunrui)
        if sc.hachu_saibunrui:
            parts.append(sc.hachu_saibunrui)
        if sc.koji_name:
            parts.append(sc.koji_name)
        
        folder_name = "_".join(parts)
        
        # 無効な文字を削除（Windowsのファイル名として使用可能にする）
        folder_name = self.file_utils.sanitize_filename(folder_name)
        
        # 空の場合はデフォルト名を使用
        if not folder_name:
            folder_name = "その他"
        
        return folder_name
```

**2. `Downloader`クラスでフォルダ構造を生成**

```python
# src/core/downloader.py

def download_files(
    self,
    file_list: List[FileInfo],
    save_dir: str,
    naming,
    progress_callback: Optional[Callable[[int, int, str], None]] = None,
    folder_name: Optional[str] = None,  # 追加: フォルダ名
) -> DownloadResult:
    """複数のファイルをダウンロード"""
    result = DownloadResult(total=len(file_list))
    save_dir_path = Path(save_dir)
    
    # フォルダ名が指定されている場合はサブフォルダを作成
    if folder_name:
        save_dir_path = save_dir_path / folder_name
        save_dir_path.mkdir(parents=True, exist_ok=True)
    else:
        save_dir_path.mkdir(parents=True, exist_ok=True)
    
    # 以下、既存のコード...
```

**3. `main.py`で検索条件を渡す**

```python
# src/main.py

def download_files(main_window: MainWindow):
    """ファイルをダウンロード"""
    # ... 既存のコード ...
    
    # Namingを初期化（検索条件を渡す）
    naming = Naming(config.naming_rule, logger, config.search_conditions)
    
    # フォルダ名を生成
    folder_name = naming.generate_folder_name()
    
    # ダウンロード実行（フォルダ名を渡す）
    result = downloader.download_files(
        filtered_files,
        config.save_paths.local,
        naming,
        progress_callback,
        folder_name=folder_name,  # 追加
    )
```

**4. 命名規則のテンプレートを更新**

```python
# デフォルトの命名規則
naming_rule = "{daibunrui}_{chubunrui}_{shoubunrui}_{saibunrui}_{koji_name}_{filename}"
```

---

### 方法2: FileInfoに検索条件を埋め込む

#### メリット
- 各ファイル情報に検索条件が紐づく
- 複数の検索条件で取得したファイルを混在させても区別できる

#### デメリット
- FileInfoの構造を変更する必要がある
- メモリ使用量が増える可能性

#### 実装手順

**1. `FileInfo`に検索条件を追加**

```python
# src/models/file_info.py

@dataclass
class FileInfo:
    """ファイル情報を保持するデータモデル"""
    url: str
    filename: str
    file_type: str
    size: int = 0
    metadata: Optional[Dict[str, Any]] = None
    page_url: str = ""
    search_conditions: Optional[SearchConditions] = None  # 追加
```

**2. ファイル抽出時に検索条件を設定**

```python
# src/core/scraper.py

def extract_file_links_from_search_results(
    self, soup: BeautifulSoup, base_url: str, file_types: List[str], 
    search_conditions: Optional[SearchConditions] = None  # 追加
) -> List[FileInfo]:
    """検索結果ページからファイルリンクを抽出"""
    # ... 既存のコード ...
    
    file_info = FileInfo(
        url=absolute_url,
        filename=filename,
        file_type=file_type,
        metadata={"title": document_name} if document_name else {},
        search_conditions=search_conditions,  # 追加
    )
```

---

### 方法3: ハイブリッドアプローチ（推奨）

方法1と方法2を組み合わせる：

1. **`Naming`クラスに検索条件を渡す**（方法1）
2. **`FileInfo`に検索条件を追加**（方法2、オプション）
3. **フォルダ構造を動的に生成**（方法1）

#### メリット
- 柔軟性が高い
- 将来的な拡張に対応しやすい
- 既存のコードへの影響が最小限

---

## 実装の詳細

### 1. ファイル名の生成ロジック

```python
def generate_filename(
    self, file_info: FileInfo, metadata: Dict[str, Any] = None, index: int = 0
) -> str:
    """ファイル名を生成: 大分類_中分類_小分類_細分類_工事名_ファイル名"""
    
    # 検索条件から分類情報を取得
    parts = []
    
    if self.search_conditions:
        sc = self.search_conditions
        if sc.hachu_daibunrui:
            parts.append(sc.hachu_daibunrui)
        if sc.hachu_chubunrui:
            parts.append(sc.hachu_chubunrui)
        if sc.hachu_shoubunrui:
            parts.append(sc.hachu_shoubunrui)
        if sc.hachu_saibunrui:
            parts.append(sc.hachu_saibunrui)
        if sc.koji_name:
            parts.append(sc.koji_name)
    
    # 元のファイル名を追加
    parts.append(file_info.filename)
    
    # 結合
    filename = "_".join(parts)
    
    # サニタイズ
    filename = self.file_utils.sanitize_filename(filename)
    
    # 拡張子を追加
    extension = file_info.get_file_extension()
    if extension and not filename.endswith(extension):
        filename += extension
    
    return filename
```

### 2. フォルダ名の生成ロジック

```python
def generate_folder_name(self) -> str:
    """フォルダ名を生成: 大分類_中分類_小分類_細分類_工事名"""
    
    if not self.search_conditions:
        return "その他"
    
    sc = self.search_conditions
    parts = []
    
    if sc.hachu_daibunrui:
        parts.append(sc.hachu_daibunrui)
    if sc.hachu_chubunrui:
        parts.append(sc.hachu_chubunrui)
    if sc.hachu_shoubunrui:
        parts.append(sc.hachu_shoubunrui)
    if sc.hachu_saibunrui:
        parts.append(sc.hachu_saibunrui)
    if sc.koji_name:
        parts.append(sc.koji_name)
    
    folder_name = "_".join(parts)
    
    # サニタイズ（Windowsのファイル名として使用可能にする）
    folder_name = self.file_utils.sanitize_filename(folder_name)
    
    # 空の場合はデフォルト名を使用
    if not folder_name:
        folder_name = "その他"
    
    return folder_name
```

### 3. ファイル名のサニタイズ

Windowsのファイル名として使用できない文字を削除：

```python
def sanitize_filename(self, filename: str) -> str:
    """ファイル名から無効な文字を削除"""
    import re
    
    # Windowsで使用できない文字: < > : " / \ | ? *
    invalid_chars = r'[<>:"/\\|?*]'
    filename = re.sub(invalid_chars, '', filename)
    
    # 先頭・末尾の空白とピリオドを削除
    filename = filename.strip(' .')
    
    # 連続するアンダースコアを1つに
    filename = re.sub(r'_+', '_', filename)
    
    # 長すぎる場合は切り詰め（Windowsの最大パス長を考慮）
    if len(filename) > 200:
        filename = filename[:200]
    
    return filename
```

---

## 実装の優先順位

### Phase 1: 基本実装（最優先）

1. `Naming`クラスに検索条件を渡せるように拡張
2. `generate_folder_name`メソッドを追加
3. `Downloader.download_files`でフォルダ構造を生成
4. `main.py`で検索条件を渡す

### Phase 2: 改善（オプション）

1. ファイル名のサニタイズを強化
2. 空の分類項目の処理を改善（例: 空の場合はスキップ）
3. フォルダ名の長さ制限を考慮

### Phase 3: 拡張（将来）

1. `FileInfo`に検索条件を追加（複数検索条件の混在に対応）
2. 設定ファイルで命名規則をカスタマイズ可能にする

---

## 注意事項

### 1. Windowsのファイル名制限

- 最大パス長: 260文字（通常）、または32767文字（長いパス名を有効化した場合）
- 使用できない文字: `< > : " / \ | ? *`
- 予約名: `CON`, `PRN`, `AUX`, `NUL`, `COM1-9`, `LPT1-9`

### 2. 空の分類項目の処理

- 空の分類項目はスキップする（例: `大分類_小分類_工事名`）
- すべて空の場合は「その他」フォルダを使用

### 3. 工事名の取得

- 検索条件の`koji_name`を使用
- 検索条件に工事名がない場合は、ファイルのメタデータから取得を試みる

---

## 実装例

### 実装前のファイル構造
```
downloads/
  ├── 公告文書_20260113_0.pdf
  ├── 電子調達システム停止期間中の調達手続きについて_20260113_1.pdf
  └── 公告文書_20260113_2.pdf
```

### 実装後のファイル構造
```
downloads/
  └── 発注機関=その他機関_工事名=橋梁工事/
      ├── 発注機関=その他機関_工事名=橋梁工事_公告文書.pdf
      ├── 発注機関=その他機関_工事名=橋梁工事_電子調達システム停止期間中の調達手続きについて.pdf
      └── 発注機関=その他機関_工事名=橋梁工事_公告文書_1.pdf
```

---

## 推奨される実装方法

**方法1（NamingクラスとDownloaderクラスを拡張）を推奨します。**

### 理由
1. 既存のコード構造を維持できる
2. 検索条件を明示的に渡せるため、テストしやすい
3. 将来的な拡張に対応しやすい
4. 実装が比較的簡単

---

**最終更新**: 2026年1月13日
